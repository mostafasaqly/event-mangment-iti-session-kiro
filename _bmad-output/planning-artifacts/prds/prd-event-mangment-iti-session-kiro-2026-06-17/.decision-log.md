# Decision Log — Event Management Platform PRD

- **2026-06-17** — Intent: **Create**. Sources: finalized brief (`brief-event-mangment-iti-session-kiro-2026-06-17/brief.md`), `requirements.md`, brainstorm (`brainstorming-session-2026-06-17-1150.md`).
- **Working mode:** Fast path (user choice). Drafted full PRD from existing inputs with `[ASSUMPTION]` tags.
- **User Journeys:** Named-persona narratives (user choice) — Mariam (organizer), Ahmed (attendee).
- **Stakes:** Real product, intentionally small V1 → internal-tool-sized PRD (~5-7 pp).
- **Carried decisions (from brief/requirements):** single flat status model (not two-axis); manual payment only (no gateway in V1); WhatsApp pre-filled links not API; certificates deferred to V2; instructor portal + multi-staff deferred; partial payments deferred; advisory (non-blocking) auto-flags.
- **Architecture / DB / UX mockups:** explicitly deferred — capabilities only.
- **Concern scan:** monetization (deferred/manual), data privacy (PII + payment screenshots), localization (Arabic/RTL), audit trail. No regulatory/compliance cluster needed for V1.

## Finalize (2026-06-17)

- **Open-item triage — resolved by user:**
  - Capacity accounting → **only CONFIRMED/EXEMPT hold seats**; pending/under-review do not reserve. (FR-10 assumption → confirmed.)
  - Group approval → **approving one linked proof confirms all N at once**. (FR-13 assumption → confirmed.)
  - Organizer auth → **email + password**; added **FR-15** (Access & Accounts). Attendee status pages stay token-based no-login.
  - Localization → **bilingual Arabic + English, RTL-ready**; added to §6.1 In Scope.
- **Deferred open questions (non-blocking):** cash-method reference handling; per-method payment-details configuration; monetization model (out of v1 build).
- **Polish:** PRD assessed as already right-sized and clean (no structural/prose cuts needed). 
- **Close:** status → `final`.

## Update (2026-06-17) — resolve remaining open questions

- **Cash payments:** reference optional; `cash` stays `PENDING` until in-person confirm; Organizer marks `CONFIRMED`/`EXEMPT` from dashboard + Internal Note; audit-logged. → new **FR-3b**, Glossary "Internal Note".
- **Payment details display:** Organizer configures one+ manual Payment Methods **per Event**; instructions shown on `PENDING` Status Page; destination details visible only to registering/paying attendees. → new **FR-1b**, FR-7 enriched, Glossary "Payment Method".
- **Monetization:** not built in v1; validated manually. Path: free pilot → per-event/per-attendee → subscription tiers → certificates add-on (v2) → WhatsApp premium (v2) → gateway (v3). → new **§7a**, Open Questions trimmed.
- All §8 v1 open questions now resolved. status remains `final`, `updated` refreshed.
